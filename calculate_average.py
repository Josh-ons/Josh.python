def calculate_average(numbers):
    total = sum(numbers)
    count = len(numbers)
    average = total / count
    return average


data = [2, 4, 6, 8, 10, 12]
answer = calculate_average(data)
print("The average is ", answer)

# create a function to display the palindrome
# create a funtion to show longest string in a list of items
